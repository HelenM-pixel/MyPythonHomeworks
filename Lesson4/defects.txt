Тест не падает, хотя должен при проверках наличия символов, которые не упомянуты в документации

# тест 3 проверка наличия символа - негативная

@pytest.mark.negative
@pytest.mark.parametrize("input_str, symbol", [
    ("no abc", " "),  # ищем пробел (а вроде не должны?)
    ("", ""),  # пустая строка
    ("abc", "ab"),  # два символа, должна вернуть True? или False и не упасть
    ("Three simple", "e"),  # многократно в предложении
])
def test_contains_negative(input_str, symbol):
    assert string_utils.contains(input_str, symbol)
    # было assert not - тест падал на всех данных (хотя не должен),
    убрала assert not и проверки проходят - символ находится, хотя не должен
    баги тут или просто не документированные возможности?